#include <stdio.h>
#include <string.h>

// 获取字符数组长度
int Mystrlen(char str[]){
    int len = strlen(str);
    return len;
}

int Mystrcmp(char str1[], char str2[]){
    int 
}

int main(){
    char str[10];
    gets(str);
    int len = Mystrlen(str);
    printf("len = %d", len);
    return  0;
}