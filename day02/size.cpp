#include <stdio.h>
#include <stdlib.h>

int main(){
    int a;
    a = 6;
    printf("sizeof(double) = %ld\n", sizeof(long double));
    printf("sizeof(a) = %ld\n", sizeof(a));
    system("pause");
    return 0;
}